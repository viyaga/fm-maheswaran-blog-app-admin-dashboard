import { asyncHandler } from "./common";

export const changePassword = asyncHandler(async (dispatch, payload) => {
  
});

export const updateProfile = asyncHandler(async (dispatch, payload) => {
  
}
);